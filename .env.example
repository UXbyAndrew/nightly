# Copy to .env.local and fill in from your Supabase project settings → API.
# Leave both blank to run fully local-first on this device (no accounts, no sync).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
